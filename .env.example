# Server-side only (NOT exposed to browser)
# Attorney password
ATTY_PASSWORD=YourAttorneyPassword

# Client password suffix (combined with client prefix)
CLIENT_PASSWORD_SUFFIX=2026

# Client password prefixes as JSON (clientSlug -> prefix)
CLIENT_PASSWORD_PREFIXES={"alvarado-pool":"Pool"}

# JWT signing secret (generate a random string, at least 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Server-side only (NOT exposed to browser)

# Attorney password
ATTORNEY_PASSWORD=YourAttorneyPassword

# Client password suffix (combined with client prefix from auth-helpers.mts)
CLIENT_PASSWORD_SUFFIX=2026

# Optional: Override default JWT secret for extra security
# JWT_SECRET=your-custom-secret-here
